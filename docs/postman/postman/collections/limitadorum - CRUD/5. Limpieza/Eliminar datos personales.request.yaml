$kind: http-request
url: "{{baseUrl}}/users-data/{{userDataId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test('Responde 204 No Content', function () { pm.response.to.have.status(204); });
    language: text/javascript
order: 1000
