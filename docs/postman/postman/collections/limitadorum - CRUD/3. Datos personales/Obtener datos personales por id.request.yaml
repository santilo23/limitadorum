$kind: http-request
url: "{{baseUrl}}/users-data/{{userDataId}}"
method: GET
scripts:
  - type: afterResponse
    code: pm.test('Responde 200 OK', function () { pm.response.to.have.status(200); });
    language: text/javascript
order: 3000
